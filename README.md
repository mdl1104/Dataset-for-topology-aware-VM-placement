# Dataset-for-topology-aware-VM-placement
Dataset for topology-aware VM placement, including datacenter topology, available server resources, and VM request information such as resource demands, NUMA configuration, and type labels. The dataset enables reproducible experiments and supports research on topology- and constraint-aware VM placement.
